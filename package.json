{
  "name": "course_sellingapp",
  "version": "1.0.0",
  "description": "- Initialize a new Node.js project\r - Add Express, jsonwebtoken to it as a dependecy\r - Create index.js\r - Add route skeleton for user login, signup, purchase a course, see course\r - Add middlewares for user and admin auth\r - Add a DB(mongodb), use dotenv to store the DB connection string\r - Define the schema for User, Admin, Course, Purchase\r - Complete the routes for user login, signup, purchase a course, see course (Extra points - Use express routing to better structure your routes)\r - Create the frontend",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.10.0"
  }
}
